[build]
builder = "NIXPACKS"
buildCommand = "npm install -g pnpm && pnpm install --frozen-lockfile"

[deploy]
startCommand = "pnpm --filter @workspace/discord-bot exec tsx src/migrate.ts && pnpm --filter @workspace/discord-bot start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
